server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/moonvies-config-repo
          default-label: main
          clone-on-start: true
